---
title: W4.2 web-rendering capability 设计
date: 2026-05-08
ref:
  - docs/00-architecture/charter.md v0.4 § 1.2 / § 1.3 (规则 C) / § 1.4 / § 2.1
  - docs/RefactorV2/audit/2026-05-08-register-and-layer-audit.md § 4 (Wave 4)
status: design draft — 待 user review
risk: 高(改动面 ~1500 行;view 域大半下沉;分多 commit 推进)
---

# W4.2 web-rendering capability 设计

> WebView 429 行只是冰山一角。整个 `src/views/web/` 域当前持有 ~1500 行代码,
> 其中 70% 是 driver-级业务编排(SyncDriver / TranslateDriver / slotBus / sync-inject)
> 错置在 view 层。本文档设计 capability 化路线,实施完成后 WebView 预计 429 → ~80 行,
> view 域只剩 React 渲染 + 命令注册 + 状态订阅。

---

## 1. 现状审计

### 1.1 src/views/web/ 完整代码盘点

| 文件 | LOC | 性质 | charter 归属 |
|---|---|---|---|
| WebView.tsx | 429 | view 主组件,但 70% 是 webview 生命周期 / SyncDriver 编排 | view 应轻 |
| WebToolbar.tsx | (未读)| UI 组件 | view ✅ |
| context-menu-integration.ts | (Wave 3 既有)| 命令注册 | view ✅ |
| web-commands.ts | 39(Wave 3.2 加)| 命令注册 | view ✅ |
| data-model.ts | 95 | per-ws 状态 hydrate/setter | view ✅ |
| index.ts | 24 | self-register 入口 | view ✅ |
| **sync/sync-driver.ts** | **448** | webview 间事件同步引擎 | ❌ **driver / capability** |
| **sync/sync-inject.js** | **381** | 注入 webview 的同步脚本 | ❌ **driver / capability** |
| **sync/sync-protocol.ts** | 20 | 协议常量 | ❌ **driver / capability** |
| **slot-bus.ts** | 122 | 跨 slot 模块级 event bus | ❌ **shared 或 capability** |
| **translate/translate-driver.ts** | **119** | Google Translate 注入引擎 | ❌ **driver / capability** |
| **translate/google-translate-inject.js** | **85** | 翻译注入脚本 | ❌ **driver / capability** |
| translate-view/TranslateWebView.tsx | 292 | 右栏视图组件,但大半同样是 webview 编排 | view 应轻 |
| translate-view/lang-defaults.ts | (未读)| 语言常量 | shared 或 view ✅ |
| translate-view/index.ts | (未读)| self-register 入口 | view ✅ |

**应下沉到 capability/driver 的代码总量:~1175 行。**view 域瘦身后预计剩 ~350 行(2 个 view 组件 + 命令 + 状态 + 命令注册)。

### 1.2 charter 对齐评估

charter § 1.3 规则 C(颗粒度):
> 即便当前只一个视图消费,只要符合"有状态封装"特征,也建立独立 Capability,
> 为未来跨视图复用预留接口。

`SyncDriver` / `TranslateDriver` / `slotBus` 全部符合"有状态 / 有生命周期"特征——所以**必须独立成 capability,不论是否有未来其他消费者**。这是规则 C 的字面要求,Wave 1~3 已经把"换底层零成本"承诺立住,W4.2 是把这个承诺真正应用到 web 域。

charter § 1.3 规则 A:
> `electron WebContentsView` | capability.web-rendering(主进程封装)
> `electron session.webRequest / webContents` | capability.browser-capability

charter 已经预留了 `web-rendering` 名字。本设计跟 charter 列表对齐。

### 1.3 driver vs capability 的二选一

V2 当前架构(eslint.config.js:46 注释)把 driver 单独拎出 capability 概念,作为"必经的业务驱动层"。`text-editing-driver` 是先例:它封装 PM(npm 包),被 view 通过 `<textEditingDriver.Host />` 使用,自身又有 `setLinkClickHandler` 等 view 注入接口。

web 域的 SyncDriver / TranslateDriver 已经命名为 driver——它们应当跟 text-editing-driver 同模式,做成:
- `src/drivers/web-sync-driver/` — 双 webview 事件同步引擎(SyncDriver + sync-inject)
- `src/drivers/web-translate-driver/` — Google Translate 注入引擎(TranslateDriver + google-translate-inject)

而**整个 webview tag 的生命周期编排 / partition 守门 / dom-ready 时序**——这部分应该是 `capability.web-rendering` 的内核(Host 组件 + 状态 + commands)。

**所以:1 个 capability(web-rendering) + 2 个 driver(web-sync-driver / web-translate-driver)**。

这跟 text-editing 域的现状不一致(text-editing 是 1 driver 没 capability),但 text-editing 是历史遗留——V2 走到现在,web 域是第一个真按 charter § 1.3 规则 C 严格归位的领域,以后 text-editing 重整也参考这个三层结构。

---

## 2. 设计目标

按 charter § 1.2 + § 1.3 + § 1.4:

1. **WebView / TranslateWebView 作为纯 view 声明**,只做组合 + 状态订阅 + 命令注册,LOC ≤ 100
2. **web-rendering capability 作为 web 域唯一对外面孔**,view 通过 `install: ['web-rendering']` 声明依赖
3. **SyncDriver / TranslateDriver 作为 driver**,web-rendering capability 内部编排它们,view 看不到
4. **`<webview>` tag 的 DOM 操作** 全部封装在 capability 内的 `<Host />` 组件
5. **slot-bus 作为内存 pub/sub 原语下沉到 shared/event-bus**(类似 Wave 3.3 ChannelHub),或归 capability 内部
6. **不破坏现有功能**:webview 浏览、双栏翻译、ContextMenu、URL bar 全部保持

非目标:
- ❌ 重写翻译协议 / 同步协议 — 字节级保持原样
- ❌ 优化 SyncDriver 的 80ms poll(留 follow-up)
- ❌ 改 V2 web view 数据模型(per-ws state schema 不动)

---

## 3. 目标架构

### 3.1 目录拓扑(改造后)

```
src/
├── shared/event-bus/
│   ├── channel.ts            (Wave 3.3 已有)
│   ├── result.ts             (Wave 3.3 已有)
│   └── slot-bus.ts           (W4.2 新增 — 从 views/web/slot-bus.ts 物理迁)
│
├── drivers/
│   ├── text-editing-driver/  (既有)
│   ├── web-sync-driver/      (W4.2 新增)
│   │   ├── index.ts          (re-export)
│   │   ├── sync-driver.ts    (从 views/web/sync/sync-driver.ts 迁)
│   │   ├── sync-protocol.ts  (从 views/web/sync/sync-protocol.ts 迁)
│   │   ├── sync-inject.js    (从 views/web/sync/sync-inject.js 迁)
│   │   └── DESIGN.md
│   └── web-translate-driver/ (W4.2 新增)
│       ├── index.ts
│       ├── translate-driver.ts
│       ├── google-translate-inject.js
│       └── DESIGN.md
│
├── capabilities/
│   ├── (Wave 1+3 既有 6 个)
│   └── web-rendering/        (W4.2 新增)
│       ├── index.ts          (capability 自注册 + api 暴露)
│       ├── Host.tsx          (<Host /> 组件 — webview tag 生命周期管理)
│       ├── translate-host.tsx (<TranslateHost /> 组件 — 翻译模式 host)
│       ├── webview-types.ts  (WebviewElement 接口)
│       └── DESIGN.md
│
└── views/web/
    ├── WebView.tsx           (~80 行 — 组合 + 状态订阅 + 命令)
    ├── WebToolbar.tsx        (UI 组件,基本不动)
    ├── translate-view/
    │   └── TranslateWebView.tsx (~80 行)
    ├── data-model.ts         (per-ws state)
    ├── web-commands.ts       (Wave 3.2 命令)
    ├── context-menu-integration.ts (既有)
    ├── lang-defaults.ts      (语言选项常量,移到 translate-view/ 上一层?待定)
    └── index.ts
```

### 3.2 责任边界

#### shared/event-bus/slot-bus.ts(从 views 物理迁)
- 跨 slot 模块级 event bus,无 view 语义
- 类似 Wave 3.3 ChannelHub,纯 pub/sub 原语
- 为 web view 用,也供未来其他场景(若有)

#### drivers/web-sync-driver/
- SyncDriver 类(双 webview 事件同步引擎)
- sync-inject.js(注入到 webview 内的脚本)
- sync-protocol.ts(action 常量)
- **接口**:`createSyncDriver(side, options) → { bind(wv), start(), destroy(), takeControl(), yield(), reinject() }`
- driver 协议铁律 5:不 import 其他 driver(本身不依赖)

#### drivers/web-translate-driver/
- TranslateDriver 类(Google Translate 注入引擎)
- google-translate-inject.js
- **接口**:`createTranslateDriver(options) → { bind(wv), inject(targetLang), destroy() }`
- 不依赖 web-sync-driver(两个 driver 各自独立)

#### capabilities/web-rendering/
**职责**:封装整个 `<webview>` tag 的生命周期 + 跟两个 driver 的协调编排

**对外 API**:
```ts
export const webRendering = {
  /** 主 Host(普通 webview)*/
  Host: ({ workspaceId, ...props }: HostProps) => ReactElement,
  /** 翻译模式 Host(disablewebsecurity + 翻译注入)*/
  TranslateHost: ({ workspaceId, ...props }: TranslateHostProps) => ReactElement,
};
```

`<Host />` 内部封装:
- webview ref 绑定 + dom-ready 守门(WebView.tsx 107-211 这一大块)
- did-navigate / did-start-loading / did-stop-loading 事件转发
- context-menu 转发(view 内仍接 callback,具体显示由 view 注册)
- SyncDriver lifecycle(WebView.tsx 213-289 这块)
- 跨平台 partition 配置(WEBVIEW_PARTITION / WEBVIEW_TRANSLATE_PARTITION)

**view 通过 props 注入:**
- `currentUrl: string`(per-ws state)
- `partition: string`(默认走 capability 自带,可 override)
- `translateMode: boolean`(决定是否启动 SyncDriver)
- `onContextMenu(payload)`(view 自己的 context menu 注册)
- `onUrlChanged(url)`(setWebUrl 写入 per-ws state)

#### views/web/WebView.tsx(目标 ≤ 100 行)
```tsx
export function WebView({ workspaceId }: WebViewProps) {
  // 1. 订阅 per-ws state(currentUrl, targetLang)
  // 2. 订阅 slotBinding(判断 isTranslateMode)
  // 3. <WebToolbar onNavigate={...} onToggleTranslate={...} />
  // 4. <webRendering.Host
  //      workspaceId={workspaceId}
  //      currentUrl={wsState.currentUrl}
  //      translateMode={isTranslateMode}
  //      onContextMenu={showWebContextMenu}
  //      onUrlChanged={(url) => setWebUrl(workspaceId, url)}
  //    />
}
```

view 不再:
- 直接 import SyncDriver / slot-bus / WEBVIEW_PARTITION
- 写 webview ref handler / dom-ready / did-navigate 编排
- 处理 SyncDriver lifecycle

view 仍然:
- React 渲染 / hooks 订阅
- Toolbar 操作处理(命令路由)
- 命令注册 / 右键菜单注册

---

## 4. 接口契约细则

### 4.1 web-rendering capability 接口

```ts
// src/capabilities/web-rendering/index.ts

import { capabilityRegistry } from '@slot/capability-registry/capability-registry';
import { Host } from './Host';
import { TranslateHost } from './translate-host';

export interface WebRenderingHostProps {
  /** 当前 ws id(用于 SyncDriver side / partition 等)*/
  workspaceId: string;
  /** 当前显示 URL(view 从 per-ws state 取,传入)*/
  currentUrl: string;
  /** 是否处于双栏翻译模式(决定 SyncDriver 是否启动)*/
  translateMode: boolean;
  /** webview tag partition(默认走 capability 配,可 override)*/
  partition?: string;
  /** 右键菜单事件 — view 自己处理 contextMenuController */
  onContextMenu?: (payload: WebContextMenuPayload) => void;
  /** URL 变化(view 写入 per-ws state)*/
  onUrlChanged?: (url: string) => void;
  /** loading 变化(view 决定是否显示 spinner)*/
  onLoadingChanged?: (loading: boolean) => void;
  /** 导航能力变化(canGoBack / canGoForward)*/
  onNavStateChanged?: (state: { canGoBack: boolean; canGoForward: boolean }) => void;
}

export interface WebRenderingApi {
  /** 主区域 Host 组件(普通 webview)*/
  Host: React.ComponentType<WebRenderingHostProps>;
  /** 翻译模式 Host 组件(disablewebsecurity)*/
  TranslateHost: React.ComponentType<TranslateHostProps>;
  /** 命令式 API:让 view 通过 ref 调 webview 操作 */
  loadURL: (workspaceId: string, url: string) => void;
  goBack: (workspaceId: string) => void;
  goForward: (workspaceId: string) => void;
  reload: (workspaceId: string) => void;
  stop: (workspaceId: string) => void;
}

export const webRendering: WebRenderingApi = { Host, TranslateHost, loadURL, goBack, goForward, reload, stop };

// Wave 1 模式
capabilityRegistry.register({ id: 'web-rendering' });
```

`loadURL` / `goBack` 等命令式 API 走"按 workspaceId 查 webview 实例"路径:capability 内维护 `Map<workspaceId, WebviewElement>`,view 通过 webRendering.loadURL(wsId, url) 调用,不需要传 ref。这个模式参考 text-editing-driver `instanceRegistry`。

### 4.2 web-rendering install 关系

WebView / TranslateWebView 在 [src/views/web/index.ts](../../../../src/views/web/index.ts) 注册时:

```ts
registerView({
  id: 'web-view',
  install: ['web-rendering', 'web-sync-driver', 'web-translate-driver'],
  // ...
});
```

注:driver id(web-sync-driver / web-translate-driver)走 Wave 1 的 KNOWN_DRIVER_IDS 白名单,**新增到 [known-driver-ids.ts](../../../../src/slot/view-type-registry/known-driver-ids.ts)**——这是 W4.2 实施时必加的 1 行。

---

## 5. 实施分阶段

W4.2 改动量大,拆 5 个独立 commit,每步独立 typecheck + vite build + lint 验证。

### C1:slot-bus 下沉到 shared/event-bus
- 物理迁移 `src/views/web/slot-bus.ts` → `src/shared/event-bus/slot-bus.ts`
- views/web/slot-bus.ts 留 `@deprecated` re-export(对齐 Wave 3.3 ChannelHub 模式)
- 现有消费者(WebView / TranslateWebView / SyncDriver)依然走老路径,行为零变化
- **验证**:typecheck + lint(slot-bus 现在在 shared,view → shared 是纵向向下,合规)
- **风险**:低,纯搬家

### C2:web-sync-driver 落 driver 层
- 物理迁移 `src/views/web/sync/` → `src/drivers/web-sync-driver/`
- 增 `src/drivers/web-sync-driver/index.ts` 暴露 SyncDriver 类 + protocol 常量
- views/web/sync/ 留 re-export 兜底
- 改 sync-driver.ts 内部 `import { slotBus } from '../slot-bus'` → `'@shared/event-bus/slot-bus'`(C1 后已可)
- **验证**:typecheck + vite build + 手测 双栏翻译同步功能
- **风险**:中(driver 之间零代码 import 由 lint 守住,但 sync-driver 当前不 import 其他 driver,合规)

### C3:web-translate-driver 落 driver 层
- 物理迁移 `src/views/web/translate/` → `src/drivers/web-translate-driver/`
- 增 `src/drivers/web-translate-driver/index.ts`
- views/web/translate/ 留 re-export 兜底
- 改 translate-driver.ts 的 import(若有)走 driver 层路径
- 加入 KNOWN_DRIVER_IDS 白名单(`'web-sync-driver', 'web-translate-driver'`)
- **验证**:typecheck + vite build + 手测 翻译注入仍工作
- **风险**:低,纯搬家

### C4:web-rendering capability 化(主菜)
- 新建 `src/capabilities/web-rendering/{index.ts, Host.tsx, translate-host.tsx, webview-types.ts}`
- 把 WebView.tsx 107-289 的 webview 编排 + SyncDriver lifecycle 搬到 `<Host />`
- 把 TranslateWebView.tsx 内对应编排搬到 `<TranslateHost />`
- capability 自注册 capabilityRegistry
- **WebView.tsx / TranslateWebView.tsx 暂不改**——这步只**新建 capability 实现**,view 还在用旧路径
- **验证**:typecheck + vite build + 新文件可被 import(但还没人用)
- **风险**:中(新文件多但跟现状无交互)

### C5:view 层迁移 + 旧路径清理
- WebView.tsx 改用 `<webRendering.Host />`(LOC 429 → ~80)
- TranslateWebView.tsx 改用 `<webRendering.TranslateHost />`(LOC 292 → ~80)
- views/web/sync/ 与 views/web/translate/ 彻底删除(re-export 兜底文件)
- views/web/slot-bus.ts 彻底删除
- views/web/index.ts 加 install: ['web-rendering', 'web-sync-driver', 'web-translate-driver']
- **验证**:typecheck + vite build + lint --max-warnings 0 + **完整功能回归**
- **风险**:高(WebView 大改,功能回归是真考验)

### 总改动评估

| 阶段 | 文件数 | 净 LOC 变化 | 主要风险 |
|---|---|---|---|
| C1 slot-bus 迁 shared | 2 | ~0(迁移) | 低 |
| C2 sync-driver 落 drivers | 4 | ~0 | 中(物理搬家不 break import 链需小心)|
| C3 translate-driver 落 drivers | 4 | ~0 | 低 |
| C4 web-rendering 新建 | 4 | +约 600(从 view 复制粘贴)| 中 |
| C5 view 层 + 清理 | 6 | -约 1000(view 瘦身 + 删 re-export 兜底)| **高** |

C5 是真正的"改动结算"——前面 4 步是把基础设施搭好,C5 一次性把 view 切过去 + 老代码删掉。

---

## 6. 验收标准(W4.2 完工时)

| 项 | 标准 |
|---|---|
| WebView LOC | ≤ 100(stretch ≤ 80)|
| TranslateWebView LOC | ≤ 100 |
| `grep "from '@views/web/sync\|from '@views/web/translate\|from '@views/web/slot-bus" src/` | 0 命中(老路径全无消费者)|
| views/web/sync/ + views/web/translate/ + views/web/slot-bus.ts | **物理删除**(re-export 兜底也不留——一次性切净,跟 Wave 3 兜底策略不同,因为消费者已被全切) |
| capabilityRegistry.count | 6 → 7 |
| KNOWN_DRIVER_IDS | + web-sync-driver, web-translate-driver |
| install-coverage 表 | web-view 行 install 列出现 'web-rendering' (capability 列)+ 2 driver(driver 列)|
| 新增 lint 违规 | 0 |
| typecheck / vite build | 通过 |
| 功能回归(逐项手测)| 普通 webview 浏览 / 双栏翻译同步 / Google Translate 注入 / 右键菜单 / Cmd+R reload / 切语言 banner |

---

## 7. 风险与决策点(请 user review)

### 风险 1:slot-bus 真的属于 shared 吗

slot-bus 当前是"模块级 event emitter"(类似 Wave 3.3 的 ChannelHub),但它带"side: 'left' | 'right'"的语义——这是 web 双栏翻译特有的概念,**严格说不通用**。

- 选项 A:slot-bus 下沉到 `shared/event-bus/`(我的方案)— 简单,但 shared 里出现 'left'/'right' slot 概念
- 选项 B:slot-bus 留在 web-rendering capability 内部 — 边界更干净,但 capability 内部又持有"内存事件总线"重复实现

我倾向 **A**——'left'/'right' 是泛 slot 概念(workspaceContainer 的 left/right slot),不只 web 用,以后跨 slot 通信场景(如 ebook 双栏对照)也能复用。slot-bus 命名本身就在反映这一点。

**问 user:同意 A 吗?**

### 风险 2:driver vs capability 的边界要不要调整

我的方案是:
- capability **web-rendering**:`<Host />` 组件 + webview tag 生命周期 + 编排
- driver **web-sync-driver**:SyncDriver 类(纯逻辑,无 React)
- driver **web-translate-driver**:TranslateDriver 类(纯逻辑,无 React)

这跟 text-editing 域不同(text-editing 是 1 driver 提供 `<Host />`,没单独 capability)。

替代方案:**只做 driver,不做 capability** — 把 `<Host />` 也放在 driver 里(`web-rendering-driver/Host.tsx`),省一层 capability。

- 选项 A(我的方案):capability + 2 driver 三层
- 选项 B:1 driver(包含 Host)+ 2 driver 平级 — 更扁平
- 选项 C:全部塞 1 个 driver `web-rendering-driver/`(SyncDriver / TranslateDriver / Host 都在内)— 最简单但破坏 driver 协议铁律 5(driver 之间零代码 import)

**charter § 1.3 规则 C 字面要求"建立独立 Capability"**——选项 A 严格遵循。
**driver 协议铁律 5 要求 driver 之间零 import**——SyncDriver 跟 TranslateDriver 现在确实独立(互不 import),选项 A / B 都可以。

我倾向 **A**(charter 规则 C 严格对齐),但 B 也讲得通(text-editing 先例)。**问 user 选哪个?**

### 风险 3:C5 是巨型 commit 还是再拆

C5 把 WebView / TranslateWebView 大改 + 删旧文件,改动跨 view + driver + capability。可以再拆:
- C5a:WebView 改用 webRendering.Host(老旧 sync 兜底文件还在)
- C5b:TranslateWebView 改用 webRendering.TranslateHost(老旧 translate 兜底文件还在)
- C5c:删除所有 re-export 兜底 + 加 install 字段

这样每步独立 typecheck,但 C5a/b 之间互独立,可并行验证。**3 个 commit 还是 1 个?**

### 风险 4:实施时间预估

W4.2 体量按 commit 估:
- C1 slot-bus:30 min
- C2 sync-driver:1 h(381 行 inject.js 的路径 + ?raw 注入需仔细)
- C3 translate-driver:30 min
- C4 web-rendering 新建:**3-4 h**(从 view 复制 + 改写 props/callbacks + Host.tsx 跟 TranslateHost.tsx 共享逻辑提取)
- C5 view 迁移:**2 h** + 完整功能回归(本身 1 h 测试)

**总计 7-9 小时连续工作**,不能一坐到底,建议分 2-3 个 session,每 session 完成 1-2 个 commit + 你一轮验证。

---

## 8. 跟 W4.1 的关系

W4.1 (keymap registrar) 跟 W4.2 互独立,可并行实施。

但 W4.2 实施期间触碰到一些可能的 keymap 加点(比如 Cmd+R 刷新 webview),如果 W4.1 已先完工,这些键直接走 keymap 字段声明;否则走旧路径(button onClick / DOM event)。

**建议:W4.1 先 W4.2 后**——W4.1 是个独立小基础设施,先完成后整个 view 层快捷键统一新模式。

---

## 9. follow-up(明确不在 W4.2 范围)

- 优化 SyncDriver 的 80ms poll(可能改成事件驱动)
- text-editing-driver 拆 capability(对齐 web-rendering 三层结构)— 大工程,留 charter v0.5+
- web-rendering capability 加 schema 字段(charter § 1.2 capability 注册时携带 schema/converters)— web 域无 schema 概念,留空
- WEBVIEW_PARTITION 等常量从 shared/constants/webview 迁到 capability 内部(算 web 域内私事,留 capability 完全主导后再说)
- google-translate-inject.js 的 ?raw 注入路径迁移时若失败,可能需要调 vite resolve(文件名不变,路径在 driver/<>/google-translate-inject.js;Vite 应自动跟随)
