# L5-B3.6 math block + math inline 完成报告

> 阶段:L5-B3.6 — NoteEditor schema 完整化 Phase A 第二步:math
> 分支:`feature/L5B3.6-math-blocks`
> 完成日期:2026-05-07
> 设计文档:[L5B3.6-math-blocks-design.md](./L5B3.6-math-blocks-design.md)
> 前置:L5-B3.5 image 已完成

---

## 1. 阶段定位

NoteEditor schema 完整化 Phase A 第二步,完成 KaTeX 渲染的块级公式 + 行内公式。
跟 L5-B3.5 image 同款"反向驱动 schema 补齐"路线 — md-to-pm 主体不动,只升级
PM_NODE_REGISTRY 标记 mathBlock/mathInline 为 ✅,markdown 转出来的 math 节点
自动从 unknown 占位升级为真实 math 节点。

---

## 2. 完成判据(对齐设计 § 6,12 条)

| # | 判据 | 状态 | 验证方式 |
|---|---|---|---|
| 1 | npm run typecheck + lint 全过 | ✅ | 实施末验证 |
| 2 | NoteEditor 加载现有 note 不破坏 | ⏳ | 待用户实测 |
| 3 | slash `/math` → 插入空 mathBlock,自动进 edit 态 | ⏳ | 待用户实测 |
| 4 | edit 态输 LaTeX → live preview 实时(防抖 200ms) | ⏳ | 待用户实测 |
| 5 | 点 mathBlock 外 / Esc → 退出 edit | ⏳ | 待用户实测 |
| 6 | 多个 mathBlock 共存 → 共享 IntersectionObserver 性能 | ⏳ | 待用户实测 |
| 7 | floating toolbar 选中 LaTeX 文本点 ∑ → 转 mathInline(latex=选中文本)| ⏳ | 待用户实测 |
| 7b | floating toolbar 无选区点 ∑ → 插入空 mathInline atom,单击编辑 | ⏳ | 待用户实测 |
| 8 | mathInline 双击 → 编辑弹窗,Enter 保存 / Esc 取消 | ⏳ | 待用户实测 |
| 9 | 复制 mathInline → 剪贴板含 `$<latex>$`(leafText)| ⏳ | 待用户实测 |
| 10 | md-to-pm 转 markdown `$$...$$` / `$...$` → 直接渲染 | ✅ | PM_NODE_REGISTRY 升级 ✅ |
| 11 | mathBlock / mathInline registry 升级 ✅ | ✅ | 已改 |
| 12 | KaTeX 字体加载(katex.min.css 含 @font-face) | ⏳ | 待用户实测;font-src CSP 已加 'self' / data: |

**总评**:**3 ✅ 静态过 + 9 ⏳ 待实测**(都是 UI/视觉行为,需 npm start)。

---

## 3. 该阶段实施

### 3.1 设计文档 + 完成报告(2 篇)

[L5B3.6-math-blocks-design.md](./L5B3.6-math-blocks-design.md) v0.1 全 A 拍板。

### 3.2 npm 依赖

| 包 | 版本 | 作用 |
|---|---|---|
| katex | ^0.16.45(V1 ^0.16.44 同系列) | LaTeX → HTML/MathML 渲染 |

### 3.3 mathBlock(2 文件)

| 文件 | 行数 | 内容 |
|---|---|---|
| `blocks/math-block/spec.ts`(新)| ~60 | NodeSpec content='text*' code:true defining:true marks:''(纯 LaTeX 源) |
| `blocks/math-block/node-view.ts`(新)| ~265 | NodeView:rendered/edit 两态 + KaTeX 缓存 + 共享 IntersectionObserver + 共享 mousedown listener |

V1 性能优化全部直迁:
- `lastRenderedLatex` / `lastPreviewLatex` 缓存
- `sharedIntersectionObserver`(rootMargin 200px,viewport 进入才渲)
- `activeMathEditors` 共享全局 mousedown 监听

砍 V1:**LaTeX 速查面板 ?  按钮**(留 L5-B+,help-panel 整套 ~520 行未迁)。

### 3.4 mathInline(2 文件)

| 文件 | 行数 | 内容 |
|---|---|---|
| `blocks/math-inline/spec.ts`(新)| ~55 | NodeSpec inline+atom,attrs.latex,leafText 暴露 `$<latex>$` |
| `blocks/math-inline/node-view.ts`(新)| ~155 | NodeView:KaTeX 行内渲染 + 缓存 + 双击编辑弹窗(input + live preview) |

砍 V1:**`syncThoughtMark`**(V2 暂无 thought mark 系统)。

### 3.5 集成(7 文件改)

| 文件 | 改动 |
|---|---|
| `Host.tsx` | +import mathBlockSpec / mathInlineSpec,加进 ENABLED_BLOCKS |
| `pm-host.css` | +~150 行 math-block / math-inline / 弹窗样式 |
| `api.ts` | +`insertMathBlockAtSelection`(slash 用)+ `insertMathInlineAtSelection`(有选区→用选中文本作 latex;空选区→空 atom 单击编辑) |
| `note-commands.ts` | +`note-view.slash-insert-math-block` / `note-view.insert-math-inline` 命令 |
| `floating-toolbar-content.ts` | +`∑` 按钮(order 70)— 选中文本 → 转 mathInline 主入口 |
| `slash-menu-content.ts` | +`Math Block`(order 140);**行内公式不在 slash**(在 floating toolbar) |
| `md-to-pm.ts` | PM_NODE_REGISTRY:mathBlock ❌→✅,mathInline ❌→✅(主体不改) |
| `index.html` | +font-src 'self' data:(KaTeX 字体 @font-face 需要) |

**入口语义区分**(用户拍板):
- **mathBlock(块级)** → slash menu `/math` — "插 block" 在 slash menu 是 V2 一致语义
- **mathInline(行内)** → floating toolbar `∑` — "改选中文字"在 floating toolbar
  跟 bold/italic/link 等 mark/inline 操作风格一致;选区为空时也工作(插空 atom)

---

## 4. 反向驱动证明(再次验证)

跟 L5-B3.5 image 一样,L5-B3.6 mathBlock / mathInline 加进 ENABLED_BLOCKS 后:
- md-to-pm 主体逻辑**没改**
- 只把 PM_NODE_REGISTRY.mathBlock / mathInline 从 ❌ 改 ✅
- markdown 含 `$$x^2 + y^2 = z^2$$` 转出来的 `{ type: 'mathBlock', content: [...] }` 节点
  **直接渲染**为真实 math block,不再走 unknown 占位

L5-B4.3.1 设计文档 § Q2 的"输出目标 schema + unknown 占位 + 反向驱动 schema 补齐"
路线在 image / math 两个阶段连续验证,可作为 table / file-block 等剩余 ❌ 节点的
推进模板。

---

## 5. 提交分布(预估,5 commits)

```
docs(L5-B3.6): math blocks 设计文档
feat(deps): npm install katex@^0.16.45
feat(driver): mathBlock + mathInline NodeSpec + NodeView(V1 直迁)
feat(driver,view,storage): math schema 注册 + insert API + slash 项 + md-to-pm registry 升级 + CSS + CSP font-src
docs(L5-B3.6): 完成报告 + state-snapshot 更新
```

总:**~480 行新代码 + ~150 行 CSS + ~40 行集成**。

---

## 6. 砍掉的 V1 部分

| V1 部分 | 行数 | 砍因 |
|---|---|---|
| help-panel/latex/(LaTeX 速查面板) | ~520 行 | 不挡核心闭环;熟悉 LaTeX 用户不需要,新用户可查文档 |
| `syncThoughtMark`(mathInline thought mark 视觉) | ~20 行 | V2 暂无 thought mark 系统 |
| `?` help 按钮 + 相关交互 | ~40 行 | 跟随 help-panel 一起砍 |

留作后续(L5-B+ 接入 thought mark / 加 LaTeX 速查面板)。

---

## 7. 修订记录

| 日期 | 版本 | 内容 |
|---|---|---|
| 2026-05-07 | v0.1 | 阶段完成报告初版,3 ✅ + 9 ⏳ 实测;反向驱动证明再次验证 |
