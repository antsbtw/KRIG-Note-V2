/**
 * web-translate-driver — driver 层入口(W4.2 C2)
 *
 * Google Translate 注入引擎(TranslateDriver)+ inject 脚本。
 * driver 自身只用 webview tag + window.electronAPI,无 view/capability 依赖。
 */

export { TranslateDriver } from './translate-driver';
