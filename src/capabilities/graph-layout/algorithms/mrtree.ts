/**
 * mrtree preset — 多根树(Mind map / 树状结构)
 */

import type { LayoutOptions } from '../types';

export const mrtreePreset: LayoutOptions = {
  algorithm: 'mrtree',
  direction: 'DOWN',
  spacing: { node: 40, layer: 50 },
};
